CC = clang++
GAME_SOURCE = ValleyOfBones.cpp
GAME_OUTPUT = ValleyOfBones.exe
LINKER_FLAGS = -lSDL2 -lSDL2_image -lSDL2_ttf -stdlib=libc++ -O3 -std=c++17
INCLUDES = -I/usr/include/SDL2 -I/usr/local/include/SDL2

all: clean valley

valley:
	$(CC) -v -Wall $(GAME_SOURCE) $(LINKER_FLAGS) $(INCLUDES) -o $(GAME_OUTPUT)

clean:
	rm -f *.exe *.o
